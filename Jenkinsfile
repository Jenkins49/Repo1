pipeline {
    agent {node {label "Slave1"}}
    
    stages {
        stage("Build"){
            steps {
            echo "Execute the Build job in pipelinetwo"
            echo "this is the file executing from jenkins file o the github"}
            
        }
    }
}
